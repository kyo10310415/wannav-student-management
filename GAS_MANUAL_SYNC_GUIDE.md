# GASスクリプト手動実行ガイド

## 問題の原因

アプリ上で表示されるレッスン日が古い場合、以下の可能性があります：

1. **GASスクリプトがGoogleカレンダーから最新データを取得していない**
2. **GASのトリガー（自動実行設定）が正しく動作していない**
3. **Googleカレンダー自体がまだ更新されていない**

## 解決方法

### 1. GASスクリプトを手動実行する

#### 手順：

1. **Googleスプレッドシートを開く**
   - 「WannaV レッスンデータ同期」スプレッドシートを開く
   - URL例: `https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit`

2. **Apps Scriptエディタを開く**
   - メニューバー: `拡張機能` → `Apps Script`

3. **実行する関数を選択**
   - 上部のドロップダウンメニューから `testIncrementalSync` を選択
   - または `incrementalSyncCalendar` を選択

4. **実行ボタンをクリック**
   - ▶ ボタン（実行）をクリック
   - 初回実行時は権限の承認が必要な場合があります

5. **ログを確認**
   - 実行が完了したら、`表示` → `ログ` で実行結果を確認
   - 以下のような出力が表示されるはずです：
     ```
     Notionから634件のTutorメールアドレスを取得
     アクセス可能なカレンダー: 33件
     アクセス可能なTutorカレンダー: 15件（Notion: 634件中）
     差分更新期間: 2026-02-19 00:00:00 ～ 2026-04-28 00:00:00
     処理中: user1@example.com のカレンダー
     処理中: user2@example.com のカレンダー
     ...
     合計: 取得50件, 新規30件, 更新20件, 削除0件
     差分更新完了
     ```

6. **スプレッドシートを確認**
   - 「レッスン予約データ」シートに最新データが反映されているか確認
   - 学籍番号 `OLTS240499-HK` の行を探して、レッスン日時が更新されているか確認

7. **アプリで「データ更新」ボタンをクリック**
   - アプリに戻り、「データ更新」ボタンをクリック
   - スプレッドシートから最新データを取得してアプリに反映

### 2. トリガー設定を確認・設定する

#### 現在のトリガー設定を確認：

1. **Apps Scriptエディタを開く**
2. 左サイドバーの **「⏰ トリガー」** アイコンをクリック
3. 既存のトリガーがあるか確認

#### トリガーが存在しない、または動作していない場合：

1. **新しいトリガーを設定する関数を実行**
   - 関数選択ドロップダウンから `setupIncrementalTrigger` を選択
   - ▶ 実行ボタンをクリック

2. **または手動でトリガーを作成**
   - 左サイドバーの「⏰ トリガー」をクリック
   - 右下の「+ トリガーを追加」ボタンをクリック
   - 以下の設定を行う：
     - **実行する関数**: `incrementalSyncCalendar`
     - **デプロイ**: `Head`
     - **イベントのソース**: `時間主導型`
     - **時間ベースのトリガー**: `分ベースのタイマー`
     - **時間の間隔**: `30分おき`
   - 「保存」をクリック

3. **トリガーが正しく設定されたか確認**
   - トリガー一覧に表示されるはずです
   - 次回実行時刻も表示されます

### 3. Googleカレンダーを確認する

1. **Tutorのカレンダーを開く**
   - 該当のTutor（きょうへい先生: `k.sakamoto@oneloopinc.net`）のカレンダーを開く

2. **レッスン予定を確認**
   - 学籍番号 `OLTS240499-HK` のレッスン予定が正しく更新されているか確認
   - 2/26 から 2/27 に変更されているか？

3. **カレンダーが更新されていない場合**
   - カレンダーが最新状態でない場合、GASを実行しても古いデータが取得されます
   - カレンダーを先に更新してから、GASを実行してください

## トラブルシューティング

### ケース1: GAS実行時にエラーが発生する

**エラー例**: `ReferenceError: SPREADSHEET_ID is not defined`

**原因**: 設定ファイルのコメントアウトが残っている

**解決方法**:
1. Apps Scriptエディタで以下の行を確認
2. コメントアウト（`//`）を削除
   ```javascript
   const SPREADSHEET_ID = 'YOUR_SPREADSHEET_ID';
   const SHEET_NAME = 'レッスン予約データ';
   const NOTION_TUTOR_API_TOKEN = 'YOUR_NOTION_API_TOKEN';
   const NOTION_TUTOR_DB_ID = 'YOUR_TUTOR_DATABASE_ID';
   ```

### ケース2: カレンダーアクセス権限エラー

**エラー例**: `アクセス可能なTutorカレンダー: 0件`

**原因**: GASスクリプトを実行しているGoogleアカウントが、Tutorのカレンダーにアクセスできない

**解決方法**:
1. TutorのカレンダーをGASスクリプト実行者と共有
2. または、TutorのGoogleアカウントでGASスクリプトを実行

### ケース3: データが更新されない

**原因**: 以下のいずれかの可能性
- GASが正常に実行されていない
- スプレッドシートには反映されているが、アプリで「データ更新」ボタンを押していない
- Tutorメールアドレスが一致していない（担任Tutorのメールアドレスとカレンダーのメールアドレスが異なる）

**解決方法**:
1. GASのログを確認して、エラーがないか確認
2. スプレッドシートの「レッスン予約データ」シートを直接確認
3. アプリで「データ更新」ボタンをクリック
4. ブラウザの開発者ツール（F12）でコンソールログを確認

## 確認チェックリスト

- [ ] GASスクリプトの設定（SPREADSHEET_ID、NOTION_TUTOR_API_TOKEN等）が正しい
- [ ] GASスクリプトを手動実行して、エラーがないことを確認
- [ ] ログに「差分更新完了」と表示される
- [ ] スプレッドシート「レッスン予約データ」に最新データが反映されている
- [ ] トリガーが正しく設定されている（30分おき実行）
- [ ] アプリで「データ更新」ボタンをクリック
- [ ] アプリの表示が更新される

## 参考情報

### GASスクリプトの更新頻度

- **トリガー設定**: 30分おき
- **更新範囲**: 過去7日分 ～ 未来60日分
- **処理時間**: 約30〜60秒

### データフロー

```
Googleカレンダー（Tutorの予約）
    ↓ GASスクリプト（30分おき自動実行）
スプレッドシート「レッスン予約データ」
    ↓ API /api/lessons/sync-from-sheet
PostgreSQLデータベース（lessons テーブル）
    ↓ API /api/lessons/month/YYYY/MM
Webアプリ表示
```

### 問題が解決しない場合

以下の情報を提供してください：
1. GASスクリプトのログ（コピー＆ペースト）
2. スプレッドシート「レッスン予約データ」の該当行のデータ
3. Googleカレンダーの予定内容
4. アプリのブラウザ開発者ツールのコンソールログ

---

**作成日**: 2026-02-26  
**最終更新**: 2026-02-26
