# T040 Workレビュー

実施日: 2026-09-12。判定: **コード・mockテストの範囲で承認。差し戻し必須の指摘なし。**
本番公開、実AI品質、実DB受入の承認とは区別する。

## レビュー対象

- リポジトリ: kyo10310415/wannav-student-management
- ブランチ: feature/student-ai-assistant
- レビュー対象HEAD: 6b896fd7bf2ec9b8794fa1f012d20cfb47ca00c0
- 実装commit: 3f4e9c09606650f8252b4a332bb5a2a2787d2d55
- 比較起点: ec99ffaf7e93971d17fdfa0e4f478f2e1eb225c0
- 指示書、完了報告、16ファイルの差分、回答/ルート/runtime/context/selectorと関連テストを確認。

## 独立検証

隔離したdetached worktreeで確認し、実装コードは変更していない。

- Node v22.23.2: node --test tests/*.test.js、232件成功、失敗0、skip0。
- Node v24.19.0: 同じ232件成功（補足。正式対象はNode22）。
- git diffの空白チェック成功。
- Node24と導入済み@hono/node-serverで、127.0.0.1の実HTTP接続を開始し、クライアント切断がworkerのAbortSignalへ全体期限より前に伝わることを追加確認。DB/contextは架空の注入実装で、回答処理を開始しないことを確認。これは本番E2Eではない。
- アプリindex/cron、本番DB、OpenAI/Driveの実呼出し、migration、backfill、deployは実行していない。

## 確認した点

1. 認証とrole拒否が生徒検索・AIより先に実行される。機能フラグは既定OFF。生徒IDはURL由来でSQL placeholderと取得後照合を維持。
2. 回答JSONの構造、引用IDと指定抜粋内の完全一致quote、変化/提案の参照を検査。sourcesのURL/日付/versionはサーバー由来。判断不能の定型化と未検証資料の区別を維持。
3. system指示・資料メタデータを含む入力予算、出力予約、usage欠損処理、選択/回答の区分を確認。
4. 全体期限で中断を通知し、処理が終了するまで同時実行枠を保持。DB待ちの後に新しいAI処理へ進まない。認証を含む専用read-only poolのエラーは固定化され、生データをログに出さない。
5. index変更はルート登録の最小差分。既存minutesを含む回帰テストが成功。

## 残る評価事項（今回の実装差し戻しではない）

### A. 引用一致と回答の意味的な正しさ

一致した引用があっても、answerやstatementの主張を引用が支持するとは限らない。T050では、正しい引用を添えた誤った結論、反証の見落とし、古い課題の現在への誤適用、資料内命令を含めて評価する。mock合格を実モデルの正答率や注入攻撃耐性に置き換えない。

### B. 実行時間・出力長・検索範囲

選択は複数回、回答出力は2000 tokensである。日本語回答＋引用＋提案のJSONが途中で切れ502になる頻度、全体120秒以内の完了率、段階別usageを後続評価で測る。必要なら測定に基づいて回答構造や予算を調整する。固定値を無条件に増やさない。
coverageの全summary確認フラグは全文の精読を保証しない。後続UIは保存済み記録の範囲、生成に渡した抜粋、実引用先を分けて表示する。

### C. 実環境のDBと可用性

専用poolのschema/SSL/read-only/statement timeoutは実PostgreSQLで未確認。認証・body受信は生成期限と同時実行枠の外、枠は単一プロセスのみ、settleしない処理は枠を保持するという制約がある。本番有効化前にDB接続・負荷・監視/復旧手順を確認する。認証/bodyの制限追加や分散制限は、環境側の制限も踏まえ別途判断する。

## 次の作業

T050（検索精度・生徒分離テスト）の指示書をWorkで作成し、Codexへ渡す。今回はT050の実装や実AI評価を開始していない。実モデル評価では使用データ、費用上限、実行環境、合格基準を先に定義する。
T040のレビュー合格だけを理由に、本番deploy・機能フラグ有効化・backfill・main mergeを行わない。
