# GASスクリプト - 今日のレッスンが取得できない問題の修正

## 🔴 問題

**症状:**
- 今日の分のカレンダーだけ取得できていない
- それ以外のカレンダーは正常に取得できている

**発生日時:** 2026/02/26

## 🔍 原因分析

### **根本原因: 時刻の扱い方**

GASスクリプトの差分更新期間の計算で、`new Date()`を使用していました。これにより、**現在時刻を基準**にイベント取得範囲が決まっていました。

**問題のコード:**
```javascript
const today = new Date();  // 例: 2026/02/26 13:47:30
const startDate = new Date(today.getTime() - 7 * 24 * 60 * 60 * 1000);  // 2026/02/19 13:47:30
const endDate = new Date(today.getTime() + 60 * 24 * 60 * 60 * 1000);   // 2026/04/27 13:47:30
```

### **具体的な問題:**

実行時刻が13:47の場合：
- **取得範囲**: 2026/02/19 13:47:30 ～ 2026/04/27 13:47:30
- **今日のレッスン例**: 2026/02/26 10:00（午前10時）
- **結果**: 午前10時のレッスンは13:47より前なので**範囲外** → 取得されない

```
今日の時間軸:
00:00 ─── 10:00 ─── 13:47 ─── 23:59
          ↑        ↑
          レッスン   現在時刻
          (範囲外)  (ここから取得開始)
```

## ✅ 修正内容

### **解決策: 今日の0時0分0秒を基準にする**

**修正後のコード:**
```javascript
const today = new Date();
today.setHours(0, 0, 0, 0);  // 今日の0時0分0秒にセット

const startDate = new Date(today.getTime() - 7 * 24 * 60 * 60 * 1000);
const endDate = new Date(today.getTime() + (60 + 1) * 24 * 60 * 60 * 1000);  // +1日で今日の23:59:59まで含める
```

### **修正後の動作:**

実行時刻が何時でも：
- **取得範囲**: 2026/02/19 00:00:00 ～ 2026/04/28 00:00:00
- **今日のレッスン例**: 2026/02/26 10:00（午前10時）
- **結果**: 午前10時のレッスンは範囲内 → **正常に取得**

```
今日の時間軸:
00:00 ─── 10:00 ─── 13:47 ─── 23:59
↑         ↑        ↑         ↑
開始      レッスン   現在時刻   終了
(範囲内)  (範囲内)  (範囲内)  (範囲内)
```

## 📋 適用手順

### **方法1: GASエディタで直接修正**

1. Google Sheets → 「拡張機能」→「Apps Script」
2. 以下のコード部分を探す（約180行目付近）:

```javascript
// 差分更新期間の設定（過去7日〜未来60日）
const today = new Date();
const startDate = new Date(today.getTime() - INCREMENTAL_DAYS_BACK * 24 * 60 * 60 * 1000);
const endDate = new Date(today.getTime() + INCREMENTAL_DAYS_FORWARD * 24 * 60 * 60 * 1000);
```

3. 以下に置き換え:

```javascript
// 差分更新期間の設定（過去7日〜未来60日）
// 重要: 今日の0時0分0秒を基準にすることで、今日のすべてのレッスンを含める
const today = new Date();
today.setHours(0, 0, 0, 0); // 今日の0時0分0秒にセット

const startDate = new Date(today.getTime() - INCREMENTAL_DAYS_BACK * 24 * 60 * 60 * 1000);
const endDate = new Date(today.getTime() + (INCREMENTAL_DAYS_FORWARD + 1) * 24 * 60 * 60 * 1000); // +1日で今日の23:59:59まで含める
```

4. 保存（Ctrl+S / Cmd+S）

### **方法2: 修正済みファイルを使用**

1. `gas-calendar-sync-incremental-fixed.js`の最新版をコピー
2. GASエディタで全コードを置き換え
3. 保存

## 🧪 テスト方法

### **Step 1: 手動実行でテスト**

```javascript
function testIncrementalSync() {
  Logger.log('========== 差分更新テスト ==========');
  syncLessonsIncremental();
}
```

1. 関数リストから `testIncrementalSync` を選択
2. 「実行」ボタンをクリック
3. ログを確認

### **Step 2: ログの確認ポイント**

修正前（問題がある場合）:
```
更新対象期間: 2026/02/19 13:47:30 ～ 2026/04/27 13:47:30
```

修正後（正常な場合）:
```
更新対象期間: 2026/02/19 00:00:00 ～ 2026/04/28 00:00:00
```

**確認ポイント:**
- ✅ startDateの時刻が `00:00:00` になっている
- ✅ endDateの時刻が `00:00:00` になっている（翌日の0時 = 今日の23:59:59を含む）

### **Step 3: スプレッドシートで確認**

1. 「レッスン予約データ」シートを開く
2. 今日の日付（2026/02/26）のレッスンを検索
3. 午前中のレッスンが含まれているか確認

**期待される結果:**
- 今日の全時間帯のレッスン（0時～23時）が取得される
- 午前中のレッスンも正常に表示される

## 📊 修正前後の比較

### **修正前:**

| 実行時刻 | 取得範囲（今日の分） | 取得できるレッスン |
|---------|-------------------|------------------|
| 09:00 | 09:00 ～ 23:59 | 09:00以降のみ |
| 13:47 | 13:47 ～ 23:59 | 13:47以降のみ |
| 18:00 | 18:00 ～ 23:59 | 18:00以降のみ |

❌ **問題**: 実行時刻より前のレッスンが取得されない

### **修正後:**

| 実行時刻 | 取得範囲（今日の分） | 取得できるレッスン |
|---------|-------------------|------------------|
| 09:00 | 00:00 ～ 23:59 | すべて |
| 13:47 | 00:00 ～ 23:59 | すべて |
| 18:00 | 00:00 ～ 23:59 | すべて |

✅ **改善**: 実行時刻に関わらず、今日のすべてのレッスンが取得される

## 🎯 影響範囲

### **修正の効果:**

1. **今日のレッスン**: すべての時間帯のレッスンが取得される
2. **過去のレッスン**: 影響なし（引き続き正常に取得）
3. **未来のレッスン**: 影響なし（引き続き正常に取得）

### **トリガー実行時の改善:**

30分ごとの自動実行でも、今日のレッスンがすべて取得されるようになります。

**例: 1日の自動実行（30分ごと）**

| 実行時刻 | 修正前 | 修正後 |
|---------|-------|-------|
| 00:30 | 00:30以降 | 00:00～23:59 |
| 09:00 | 09:00以降 | 00:00～23:59 |
| 13:47 | 13:47以降 | 00:00～23:59 |
| 18:00 | 18:00以降 | 00:00～23:59 |
| 23:30 | 23:30以降 | 00:00～23:59 |

## ⚠️ 注意事項

### **タイムゾーンの扱い:**

GASは`Session.getScriptTimeZone()`を使用しているため、日本時間（JST）で動作します。

```javascript
function formatDate(date) {
  return Utilities.formatDate(date, Session.getScriptTimeZone(), 'yyyy/MM/dd HH:mm:ss');
}
```

### **境界値の処理:**

- **開始日**: 7日前の0時0分0秒から
- **終了日**: 60日後の翌日0時0分0秒まで（実質60日後の23:59:59まで）

## ✅ チェックリスト

修正適用後、以下を確認してください：

- [ ] GASコードを修正した
- [ ] `testIncrementalSync`でテスト実行成功
- [ ] ログで取得範囲の時刻が `00:00:00` になっている
- [ ] スプレッドシートに今日の午前中のレッスンが表示される
- [ ] 既存のトリガーが引き続き動作している

## 📝 今後の対応

### **定期的な確認:**

- 週1回: スプレッドシートで今日のレッスンがすべて表示されるか確認
- 月1回: 実行ログを確認してエラーがないかチェック

### **追加のトラブルシューティング:**

もし今日のレッスンが取得されない場合：

1. **カレンダーの共有設定を確認**
   - Tutorカレンダーがスクリプト実行者と共有されているか

2. **イベントの説明文を確認**
   - 学籍番号が正しく記載されているか（`OLTS形式`）

3. **実行ログを確認**
   - エラーメッセージがないか
   - 取得イベント数が0でないか

## 🎉 期待される結果

修正後、以下のように動作します：

```
========== レッスン差分同期開始 ==========
更新対象期間: 2026/02/19 00:00:00 ～ 2026/04/28 00:00:00
...
[1/15] tutor1@example.com: 25件のイベントを取得
  → 今日の午前中のレッスンも含まれる
...
差分検出: 新規8件、更新3件、削除1件
========== 差分同期完了 ==========
```

スプレッドシート「レッスン予約データ」に、今日の全時間帯のレッスンが表示されます。
